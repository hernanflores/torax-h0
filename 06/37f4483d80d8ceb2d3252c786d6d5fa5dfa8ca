Task: El coste del snapshot, medido y no supuesto (NFR2)

Última tarea de la Fase 3 del track modulation_20260906.

Ficheros:
- Packages/MIDI/Tests/MIDITests/CycleSnapshotCostTests.swift (dos tests nuevos)

LA CIFRA, que es el punto de la tarea:

  Pattern con modulación   27.936 bytes   load() ~613 ns   0,0031% de la ventana
  Pattern en main          27.936 bytes   load() ~695 ns
  Cycle                       144 bytes   (2 de ellos, la Modulation)

La modulación NO CUESTA NI UN BYTE. Los dos bytes de Modulation caben en el
relleno que el Cycle ya tenía, comprobado midiendo main en la misma máquina con
un worktree temporal. Es mejor que el 1% que NFR2 presuponía.

Y es la justificación retrospectiva de guardar un Int8 y no un Accent: con una
palabra entera el campo no habría cabido en el hueco y el snapshot habría
crecido de verdad. La decisión de la Fase 2 se paga aquí.

La serie, para que siga siendo comparable:
  2026-08-31   2,25 KB     274 ns    antes de los Cycles
  2026-09-01   ~37 KB      ~870 ns   con los 16 Cycles, sobre 16 Tracks
  2026-09-08   27.936 B    ~613 ns   12 Tracks x 16 Cycles, con modulación

(El salto de ~37 KB a ~28 KB no es de esta rebanada: es la bajada a doce Tracks
del 2026-09-02. La cifra de ~37 KB se midió sobre el ProbePattern de dieciséis,
que sigue en el fichero y sigue midiéndose.)

El segundo test comprueba que el campo no rompió la trivialidad del snapshot
REAL —Modulation, Cycle y Pattern siguen POD—, que es lo que hace legal copiarlo
en el hilo del scheduler. Los tests de la Fase 3 en Engine ya lo comprobaban
sobre los tipos; este lo comprueba desde el lado que de verdad lo copia.

Lo que se afirma por assert es el presupuesto —el 1% de la ventana de 20 ms— y
no la cifra: fijar los nanosegundos exactos fallaría en cualquier máquina
distinta, y fijar el tamaño convertiría una mejora de Engine en un fallo.

Verificación:
  swift test --package-path Packages/MIDI (partición de CI)   21 + 722, 0 fallos
Sin medición de jitter: NFR3, y suspendida desde el 2026-09-02.

--- VERIFICACIÓN DE FASE 3: EL Cycle LLEVA SU Modulation ---

Comandos de test:
  swift test --package-path Packages/Engine --enable-code-coverage
  swift test --package-path Packages/MIDI --filter 'VirtualLoopbackTests|JitterHarnessTests|CoreMIDIOutputTests|CoreMIDIInputTests'
  swift test --package-path Packages/MIDI --skip VirtualLoopbackTests --skip JitterHarnessTests --skip CoreMIDIOutputTests --skip CoreMIDIInputTests
  xcodebuild build -scheme ToraxH0 -destination 'generic/platform=iOS'

Resultado: Engine 865 tests, 0 fallos (846 al abrir la fase, +19 en tres tareas,
una de ellas enmendada). MIDI 21 + 722, 0 fallos. App BUILD SUCCEEDED.

Cobertura: Modulation.swift 100%, Shape.swift 100%, ProjectRecord.swift 99,10%
de líneas. Engine en total 98,61%, contra el umbral de 90%.

Ficheros de código de la fase y su test:
  Shape.swift          -> ModulationInCycleTests
  ProjectRecord.swift  -> ModulationRecordTests, RecordRoundTripTests

El número de la fase: el Pattern pesa 27.936 bytes con la modulación dentro y
27.936 sin ella (main, misma máquina). Cero coste — los dos bytes caben en el
relleno que el Cycle ya tenía. load() ~613 ns, 0,0031% de la ventana de 20 ms.

Jitter: no aplica (NFR3), y suspendido desde el 2026-09-02.

Pasos de verificación manual propuestos:
1. git show 309a2ba — el campo entra por default en el inicializador y en
   Cycle.with(...); test en las dos direcciones.
2. git show 1223a21 — la persistencia, tarea que NO estaba en el plan: sin ella,
   guardar un Bank perdía waveform y accent en silencio.
3. waveform se guarda por clave y no por índice; schemaVersion sigue en 1.
4. testAFileWithoutTheKeysReadsAsTheNeutralModulation prueba que un Bank escrito
   antes de la rebanada sigue abriendo y suena igual.
5. git show 0637f44 — la medición del snapshot.

Hallazgo registrado: el test que enumera las claves del CycleRecord se dispara
cuando SOBRA una clave, no cuando FALTA un campo del Cycle. Lo que destapó la
pérdida silenciosa fue el round trip del Cycle entero. Anotado en el propio test.

Confirmación del usuario: «ok» — aprobado sin cambios.
