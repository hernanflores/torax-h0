Task: Enchufar la regla de la Fase 2 (Fase 4, pattern-copy_20260910)

`PatternGrid` holds a `PatternChord` and feeds it `pressing`, `releasing` and
`cancelling`, but **only while the transport runs** (FR15). Stopped, a touch
still selects on the way up exactly as before: there the single tap loads the
Pattern immediately, so the first finger's `down` would have changed the material
before anyone could know the touch was a chord.

`.select` calls `selectPattern(_:)`; `.copy` calls the model's
`copyPattern(from:to:)`, which copies and loads the clipboard with the origin
(FR19). Copying writes material and nothing else — it does not arm, does not move
the selection, and touches neither the transport nor `pendingAdoption` (FR18), so
what sounds does not notice.

Files: App/BanksScreen.swift

Verification: `xcodebuild build -scheme ToraxH0 -destination
'generic/platform=iOS'` — BUILD SUCCEEDED. `swift test --package-path
Packages/Engine` — 915 tests, 0 failures. Built and installed on the simulator,
which **cannot verify this**: multitouch does not exist there (NFR6). The chord
is checked on the iPad.

---

Phase Verification & Checkpoint — FASE 4: LA CAPA DE TOQUES

Automated: xcodebuild build -scheme ToraxH0 -destination 'generic/platform=iOS'
→ BUILD SUCCEEDED. swift test --package-path Packages/Engine → 915 tests, 0
failures. Nothing further can be automated here: multitouch does not exist on
the simulator (NFR6), and this phase is exactly that.

Manual verification on the iPad with the BeatStep Pro, all eight steps confirmed:
  1. Stopped, a touch selects and the cell dims under the finger — the pressed
     highlight now drawn by hand.
  2. A touch lifted outside its cell selects nothing (FR21).
  3. copy, paste and clear still behave as in Fase 3.
  4. Running, a single tap still arms for the next bar, countdown and `queued`.
  5. **Holding 01 and touching 03 copies at once, and 01 keeps sounding.**
  6. Both release orders give the same result (FR17).
  7. The chord arms nothing: no countdown, no `queued` cell (FR18).
  8. After a chord, the next single tap arms normally — the machine starts fresh.

User confirmation: "1, 2 y 3 OK. 4, 5, 6, 7 y 8 ok".

This was the phase the plan called the risk of the track: sibling SwiftUI
Buttons never see simultaneous touches, so the grid had to resolve its own. It
holds on the hardware.
