Tarea: Anotar en `product.md` que el reloj deja de ser solo interno (Fase 1, NFR5)

Qué cambia
- Nota fechada 2026-09-03 en *MVP Scope — v1*, junto a la línea «Transporte
  (play/stop) y reloj interno».

Por qué
La línea del MVP no deja de ser cierta —el reloj interno existe— pero se queda
corta: la app pasa a poder seguir a un maestro externo, y el tempo interno deja
de ser el literal de 120 BPM. La nota lo dice sin borrar la promesa original, que
es como el resto de la página registra sus cambios.

Lo que la nota deja explícito, para que no se lea de más: la sincronía va en un
solo sentido. La app no emite clock y ser maestro es una decisión que no está
tomada — es la limitación 2 de la spec del track.

Ficheros
- conductor/product.md (modificado)

---

Verificación de fase — FASE 1: LAS DOS DESVIACIONES QUEDAN ESCRITAS

Tests automáticos (rama feat/external-clock)
- swift test --package-path Packages/Engine → 427 tests, 0 fallos
- swift test --package-path Packages/MIDI --filter 'VirtualLoopbackTests|JitterHarnessTests|CoreMIDIOutputTests|CoreMIDIInputTests' → 21 tests, 1 omitido, 0 fallos
- swift test --package-path Packages/MIDI --skip VirtualLoopbackTests --skip JitterHarnessTests --skip CoreMIDIOutputTests --skip CoreMIDIInputTests → 437 tests, 0 fallos
Sin rastro del flake `clientCreationFailed(-50)`.

Cobertura: no aplica. La fase no toca código — solo conductor/tech-stack.md y
conductor/product.md.

Verificación manual propuesta
1. Leer la enmienda del 2026-09-03 en tech-stack.md: qué cambia (tempo no fijo,
   origen corregible en vuelo), qué se conserva (el look-ahead), la alternativa
   descartada, y la fila «Reloj» del Resumen.
2. Leer la nota del 2026-09-03 en product.md: no contradice la línea del MVP,
   menciona el tempo interno editable y deja claro que la app no emite clock.

Confirmación del usuario: «Aprobado» (2026-09-03).
