Task: Reconexión y modificadores atascados (Fase 4, ctrl-all_20260905)

QUE SE HIZO
FR15, con 6 tests escritos primero (Red: 3 tests fallando).

FICHEROS
- Packages/MIDI/Sources/MIDI/ControlInput.swift: releaseModifiers() reescrito;
  releaseTemp y releaseCtrlAll ganan el parametro `publishing`.
- Packages/MIDI/Tests/MIDITests/CtrlAllReleaseModifiersTests.swift (nuevo).

DOS DECISIONES, LAS DOS DE ORDEN
1. TEMP SE RESTAURA PRIMERO. Misma prioridad que rige mientras los dos estan
   hundidos (FR13). Al reves, Ctrl All escribiria sus bases sobre un Pattern que
   todavia lleva el fill de Temp encima, y el fill quedaria escrito — que es
   exactamente lo que los dos snapshots existen para impedir.
2. PUBLICA COMO MUCHO UNA VEZ. Los dos releases aceptan publishing: false y la
   publicacion la hace quien cierra. Dos snapshots seguidos por el PatternHandoff
   no romperian nada, pero el primero enseñaria un estado intermedio —el Pattern
   con Temp devuelto y Ctrl All todavia puesto— que nunca existio para el usuario.

POR QUE LA RESTAURACION VIVE AQUI Y NO EN UN METODO APARTE
Es la razon que temp-parameters ya dejo escrita y que aqui pesa mas: el sitio
donde habria que acordarse de llamar a una restauracion separada es precisamente
el de la reconexion, que nadie prueba a mano.

TESTS Y COBERTURA
  swift test --package-path Packages/MIDI -> 597 tests, 7 fallos, todos
  VirtualLoopback (flake midi-test-flake_20260826).
  TempReleaseModifiersTests sigue en verde pese a que releaseTemp cambio de firma.
  MIDI total: 84,23% de lineas (umbral >=80%).
  ControlInput.swift: 100% de lineas y de funciones.
swift format limpio.

--- VERIFICACION DE FASE 4 (checkpoint) ---

TESTS AUTOMATICOS
  swift test --package-path Packages/Engine  -> 534 tests, 0 fallos
  swift test --package-path Packages/MIDI    -> 597 tests, 7 fallos, todos
    VirtualLoopback (flake midi-test-flake_20260826)
  xcodebuild build -scheme ToraxH0 -destination generic/platform=iOS -> SUCCEEDED
  swift format limpio.

COBERTURA
  MIDI total: 84,23% de lineas (umbral >=80%)
  ControlInput.swift: 100% de lineas y de funciones

COBERTURA DE TESTS PARA LOS CAMBIOS DE LA FASE
Diff cfeb5a6..HEAD: un solo fichero de codigo, ControlInput.swift, con cinco
ficheros de test nuevos y 40 casos: CtrlAllModifierInputTests (12),
CtrlAllTakesOverInputTests (7), TempAndCtrlAllTests (7), CtrlAllTouchPathTests
(8), CtrlAllReleaseModifiersTests (6).

VERIFICACION MANUAL PROPUESTA
Primera fase que se puede tocar en el iPad.
1. Preset v3 cargado, encoders en Relative #2.
2. Step 14 + Velocity: los doce suben conservando el balance; soltar devuelve.
3. Desplaza y no iguala: dos Tracks con Pulses distintos mantienen su diferencia.
4. El tope: giro largo contra el limite y vuelta, sin knob muerto.
5. El step 14 es momentary y no toggle (supuesto de FR1).
6. FR13: 13+14 suena como Temp; soltar el 13 sin soltar el 14 pasa a Ctrl All;
   soltar el 14 primero no deshace el Temp antes de tiempo.
7. El corte: step buttons, pads, knob del Cycle y las vias tactiles callan.
8. Reconexion con el 14 hundido: el desplazamiento no se queda puesto.
9. Con el transporte parado, igual.
10. La pantalla todavia no distingue Ctrl All: eso es la Fase 5.

CONFIRMACION DEL USUARIO
Confirmado ("si") el 2026-09-05.
