Tasks: `copy` (FR5, FR6), `paste` (FR7–FR11) and Sustituir `copy here` por `copy`
y `paste` en `PatternGrid` (Fase 3, pattern-copy_20260910)

Three plan tasks in one commit, deliberately: removing `copy here` and adding
`copy`/`paste` is one change of signature on `PatternGrid`, and splitting it
would leave a commit that does not compile.

**This closes the reported defect.** With the transport stopped, `copy` in one
slot and `paste` in another now change the content — which `copy here` never
did — and the clipboard holds the whole Pattern, so it crosses Banks.

- `copyPattern()` loads the current slot of the current Bank. It reads the
  material saved in the Bank and not `pattern`, the live copy, so a Temp or Ctrl
  All overlay in flight is not frozen into a slot (FR6) — the same rule
  `recordEdit()` already follows. Available stopped and running (FR5).
- `pastePattern()` writes into the Bank being looked at, whatever Bank the
  material came from (FR7). The destination comes from
  `PatternClipboard.destination(...)` (FR8). It moves no selection, arms
  nothing, touches neither the transport nor `pendingAdoption` (FR10), and
  triggers the Bank autosave (FR11).
- `copyPattern(from:to:)` is the chord's entry point: it copies and loads the
  clipboard with the origin (FR19). Nothing calls it until Fase 4.
- `copyingSelectedPattern(to:)` is now gone (FR23) — the decision Fase 1
  deferred. Its App caller was the defect. `PatternCopyTests` keeps what was
  only its own (clearing, and neither operation moving the selection) and hands
  the copy cases to `PatternCopyWithOriginTests`, which state the origin.
- `paste` is disabled while the clipboard is empty (FR4). `clear` is untouched,
  as the spec's Out of Scope asks.

Files: App/TransportModel.swift, App/BanksScreen.swift,
Packages/Engine/Sources/Engine/Project.swift,
Packages/Engine/Tests/EngineTests/PatternCopyTests.swift

Verification: `swift test --package-path Packages/Engine` — 915 tests, 0
failures (two cases fewer: the retired method's). `xcodebuild build -scheme
ToraxH0 -destination 'generic/platform=iOS'` — BUILD SUCCEEDED.

---

Phase Verification & Checkpoint — FASE 3: PORTAPAPELES, `copy` Y `paste`

Automated tests:
  swift test --package-path Packages/Engine --enable-code-coverage
  → 915 tests, 0 failures (two fewer: the retired method's cases)
  Coverage: Engine 98.70% of lines, PatternClipboard.swift 100% — threshold ≥90%.
  xcodebuild build -scheme ToraxH0 -destination 'generic/platform=iOS' → BUILD SUCCEEDED
  Also built, installed and launched on the iPad Pro 11-inch simulator; the app
  runs. The simulator takes no taps from the command line, so the grid itself was
  checked by the user.

Manual verification, all six steps confirmed by the user:
  1. banks tab, transport stopped.
  2. Three buttons under the grid — copy, paste, clear — with paste disabled
     while the clipboard is empty (FR4).
  3. Slot 01 selected, copy pressed, paste becomes enabled.
  4. **The original defect check**: slot 09 (empty) selected, paste pressed, and
     09 turns `ready`. This is what `copy here` never did.
  5. Across Banks: copied in Bank 1, switched to Bank 5, pasted into an empty
     slot, and the slot fills. This is what the clipboard adds and the chord
     cannot do.
  6. `clear` unchanged, as the spec's Out of Scope asks.

User confirmation: "1. OK 2. OK 3. OK 4. OK 5. OK 6. OK".

The reported defect is closed here. Fases 4 and 5 add the chord and the visual
signals; if they never existed, what is delivered works and nothing is half done.
