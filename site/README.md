# Torax H-0 site

Static one-page site for GitHub Pages. No build step or server runtime is required.

## GitHub Pages

1. Push the contents of this folder to a repository.
2. In **Settings → Pages**, choose **Deploy from a branch**.
3. Select the branch and the repository root (`/`).

The entry point is `index.html`.
